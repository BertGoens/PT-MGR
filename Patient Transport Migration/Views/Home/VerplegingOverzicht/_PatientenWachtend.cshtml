@model Patient_Transport_Migration.Models.VM.VerplegingOverzicht.PatientenTeVerplaatsenVM

<table class="table table-bordered table-hover table-responsive table-striped">
    <caption><big><span class="glyphicon glyphicon-refresh glyphicon-clickable" onclick="LoadPatientenWachtend()"> </span>
        Patienten wachtend op vervoer</big>
    </caption>
    <tr>
        <th>Datum Vervoersaanvraag</th>
        <th>Patient</th>
        <th>Kamer - Bed</th>
        <th>Moet naar</th>
    </tr>

    @foreach (var taak in Model.PatientenWachtend) {
        <tr>
            <td>@taak.DatumGemaakt</td>
            <td>@taak.Aanvraag.Patient.Naam</td>
            <td>@taak.Aanvraag.Patient.Kamer - @taak.Aanvraag.Patient.BedNr</td>
            <td>@taak.LocatieEind.Omschrijving - @taak.LocatieEind.Kamer</td>
        </tr>
    }

</table>

<ul class="pagination">
    @for (var page = Model.Pager.StartPage; page <= Model.Pager.EndPage; page++) {
        <li class="@(page == Model.Pager.CurrentPage ? "active" : "")">
            <a href="#" onclick="LoadPatientenWachtend(@page)">@page</a>
        </li>
    }
</ul>
