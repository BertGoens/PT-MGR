@model Patient_Transport_Migration.Models.VM.VerplegingOverzichtVM
<!-- TODO VerplegingOverzicht View -->
<table class="table">
    <tr>
        <th>Datum Vervoersaanvraag @*TODO ?Verwacht ophaaltijdstip-->*@</th>
        <th>Patient</th>
        <th>Kamer</th>
        <th>Bed</th>
    </tr>

    @foreach (var taak in Model.PatientenWachtendVM.PatientenWachtend) {
        <tr>
            <td>
                @taak.DatumGemaakt

                @*
                    @{
                        DateTime g = @taak.DatumGemaakt;
                        if (@taak.GeschatteTijdNodigInSeconden.HasValue) {
                            var tijdnodig = @taak.GeschatteTijdNodigInSeconden.Value;

                            Html.Raw(g.AddSeconds(tijdnodig));
                        }
                    }
                *@
            </td>
            <td>@taak.Aanvraag.Patient.Naam</td>
            <td>@taak.Aanvraag.Patient.Kamer</td>
            <td>@taak.Aanvraag.Patient.BedNr</td>
        </tr>
    }

</table>
